
#include <stdio.h>

int main()
{
   int c;
   
   while ((c = getchar()) != EOF) {
       putchar(c);
   }
   
    return 0;
}

//c = getchar() != EOF should be 0 or 1 because its a boolean value 
// need parentheses because != has higher precendence than =
